import React from 'react';

const UserProfile = () => {
    return (
        <div>
            <h2>Auth App: User Profile</h2>
            <p>This is the User Profile</p>
        </div>
    );
};

export default UserProfile;